import { DashboardView } from './_components/DashboardView';

/**
 * Dashboard — landing pós-login da org ativa. Server Component fino;
 * todo o estado (me, org ativa, contagens) vive em <DashboardView />.
 */
export default function DashboardPage() {
  return <DashboardView />;
}
